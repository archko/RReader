export struct PageData {
    x: float,
    y: float,
    width: float,
    height: float,
    image: image,
    page_index: int,
}

export struct UIRecent {
    title: string,
    path: string,
    thumbnail: image,
    has_thumbnail: bool,
    page: int,
}

export struct HistoryRow {
    items: [UIRecent],
}

export struct UIOutlineItem {
    title: string,
    page: int,
    level: int,
}
